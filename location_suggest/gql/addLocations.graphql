mutation ($user_id: String!, $name: LocationNameInput!, $latitude: Float!, $longitude: Float!) {
  addLocation(
    user_id: $user_id
    name: $name
    latitude: $latitude
    longitude: $longitude
  )
}