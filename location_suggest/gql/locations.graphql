query ($user_id: String!) {
    locations (user_id: $user_id) {
        location {
            latitude
            longitude
        }
        elevation
        avg_temp
        trewartha
        climate_zone
    }
}